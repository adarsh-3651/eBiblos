// Importing React to use JSX syntax and create a component
import React from 'react'

// Defining a functional component named PricingPage
function PricingPage() {
  return (
    // JSX returns a simple div with the text "PricingPage"
    <div>PricingPage</div>
  )
}

// Exporting the component so it can be used in other parts of the application
export default PricingPage
