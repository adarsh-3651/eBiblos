// Importing the React library to use JSX
import React from 'react'

// Defining a functional component named Features
function Features() {
  return (
    // JSX that returns a simple <div> with the text "Features"
    <div>Features</div>
  )
}

// Exporting the Features component for use in other parts of the app
export default Features
